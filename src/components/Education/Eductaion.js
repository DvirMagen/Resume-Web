export default function Eduction({label, academy, years})
{
    return (
            <div>
                <h2>{label}</h2>
                <h3>{academy}</h3>
                <p>{years}</p>
            </div>
    );
}